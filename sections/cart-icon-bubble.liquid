

{%- render 'icons' with icon, icon: 'handbag', class: 'icon icon-cart', attr: 'width="19" height="20"' -%}
<span class="header-cart__span d-none">{{ 'general.cart.cart_title' | t }}</span>
<span class="header-cart__count cart-count-bubble rounded-circle" data-cart-count>{{ cart.item_count }}</span>
<span class="header-cart__span d-none">{{ 'general.cart.cart_items_count' | t:count:cart.item_count }}</span>
<span class="header-cart__span d-none" data-cart-cost>{{ cart.total_price | money }}</span>


{% comment %}

This file has been rewritten to preserve the original behavior.

---
Changes:
- from: '{%- render ''icons'' with icon: ''handbag'', class: ''icon icon-cart'', attr:
    ''width="19" height="20"'' -%}'
  to: '{%- render ''icons'' with icon, icon: ''handbag'', class: ''icon icon-cart'',
    attr: ''width="19" height="20"'' -%}'
  position: 2..108
{% endcomment %}